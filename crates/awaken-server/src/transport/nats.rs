//! NATS transport integration (feature-gated behind "nats").

// This module will contain NATS-based transport bindings.
// Currently a compile-gate stub — implementation requires async-nats runtime integration.
